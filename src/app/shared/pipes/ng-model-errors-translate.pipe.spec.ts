import { NgModelErrorsTranslatePipe } from './ng-model-errors-translate.pipe';

describe('NgModelErrorsTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new NgModelErrorsTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
