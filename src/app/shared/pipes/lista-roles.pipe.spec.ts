import { ListaRolesPipe } from './lista-roles.pipe';

describe('ListaRolesPipe', () => {
  it('create an instance', () => {
    const pipe = new ListaRolesPipe();
    expect(pipe).toBeTruthy();
  });
});
